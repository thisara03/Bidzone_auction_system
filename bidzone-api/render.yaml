# Optional Render Blueprint: set repo in the Render dashboard, or merge into your root blueprint.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: bidzone-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /alive
