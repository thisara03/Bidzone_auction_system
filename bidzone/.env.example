# Copied to .env for local dev. Vite only exposes vars that start with VITE_.

# Your deployed API (no trailing slash), e.g. https://bidzone-api.onrender.com
# Leave empty to use same-origin requests only (needs a reverse proxy in prod).
VITE_API_URL=
